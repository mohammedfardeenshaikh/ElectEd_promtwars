# Skill: Generate Code

Act as the Lead Engineer. Build the Election Assistant using the following constraints:
1. **Google Integration:** Use `googleapis` for Civic Info and `@google/generative-ai` for Gemini.
2. **Component Structure:** Use a modular approach (e.g., `TimelineView`, `ChatInterface`, `LocationPicker`).
3. **Efficiency:** Implement debouncing for address searches to minimize API calls.
4. **Clean Code:** Use TypeScript for type safety and clear naming conventions.
